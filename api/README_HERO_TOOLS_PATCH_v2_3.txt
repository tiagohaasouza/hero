Patch v2.3
- View /tools com título "Hero Tools", layout melhor e health-check (online/offline) via navegador.
- Comando hero:tools:install agora cria backup de .env e docker-compose.yml ANTES de instalar.
- O install também garante variáveis de porta no .env (ADMINER_PORT, PHPMYADMIN_PORT, etc) com valores padrão, se ausentes.
- Mantidos phpMyAdmin e correções de URLs.
